<h1>My Tasks</h1>
<% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %> <%= link_to "Delete #{task.title}", task_path(task), method: :delete, data: { confirm: "Are you sure?"} %></li>
<% end %>
<p><%= link_to 'Add new Task', new_task_path %></p>
